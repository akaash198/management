# Copy to /opt/flowteam/.env.prod and fill in values.
POSTGRES_DB=flowteam
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me

# Comma-separated hostnames/IPs Django will accept requests for
ALLOWED_HOSTS=app.cowrkflow.com,localhost,127.0.0.1

# Django secret key (generate with: python -c "import secrets; print(secrets.token_urlsafe(50))")
SECRET_KEY=change-me

# CORS — frontend URL the browser will be loaded from
CORS_ALLOWED_ORIGINS=https://app.cowrkflow.com

# Email configuration (SMTP)
DEFAULT_FROM_EMAIL=noreply@cowrkflow.com
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-smtp-user
EMAIL_HOST_PASSWORD=your-smtp-password
EMAIL_USE_TLS=True

# Frontend URL (for email links, OAuth redirects)
FRONTEND_BASE_URL=https://app.cowrkflow.com

# VAPID keys for web push (generate with: python -c "from pywebpush import generate_vapid_keys; import json; print(json.dumps(generate_vapid_keys()))")
VAPID_PUBLIC_KEY=change-me
VAPID_PRIVATE_KEY=change-me
VAPID_CLAIMS_SUB=mailto:admin@cowrkflow.com

# OAuth providers (fill in your client IDs/secrets)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://app.cowrkflow.com/api/auth/oauth/google/callback/
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_REDIRECT_URI=https://app.cowrkflow.com/api/auth/oauth/github/callback/
GITLAB_CLIENT_ID=
GITLAB_CLIENT_SECRET=
BITBUCKET_CLIENT_ID=
BITBUCKET_CLIENT_SECRET=

# Monitoring
PROMETHEUS_ALLOWED_IPS=127.0.0.1,10.0.0.0/8

# Optional: tweak ASGI server
USE_UVICORN=1
UVICORN_WORKERS=2

# Optional: Sentry for error tracking
SENTRY_DSN=
